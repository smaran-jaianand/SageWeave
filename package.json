{
  "name": "codelearn",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20.17.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
